export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}

export const DEFAULT_CALORIES_LIMIT = 2100;

export const TOKEN_TYPE = 'ACCESS_TOKEN';

export const AUTH_STRATEGY = 'jwt';

export const HASHING_ALGORITHM = 'RS256';

export const PAGINATION_PAGE_LIMIT = 10;